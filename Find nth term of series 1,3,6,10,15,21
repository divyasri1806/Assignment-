
class Solution {
  public:
    int findNthTerm(int N) {
        int ans = (N*(N+1))/2;    // Formula to get nth term of the Series.
        return ans;    // Return the nth term of the series.
    }
};
